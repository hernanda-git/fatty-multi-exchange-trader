# Security Boundary

Secrets are environment-only and must be redacted before persistence or notification. The analyzer boundary is designed to receive media and prompt inputs only; it never receives exchange credentials. LIVE is enabled on the Bitget lane with a bounded canary cap, native SL/TP only from confirmed fill quantity, and manual operator mutations disabled.
