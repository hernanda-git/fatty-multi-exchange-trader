# Security Boundary

Secrets are environment-only and must be redacted before persistence or notification. The analyzer boundary is designed to receive media and prompt inputs only; it never receives exchange credentials. LIVE is prohibited by default and not enabled by this repository's development runtime.
